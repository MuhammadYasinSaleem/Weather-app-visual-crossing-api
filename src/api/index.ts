import axiosInstance from './axiosInstance';
import {
  weatherService,
  getWeather,
  getCurrentWeather,
  getHourlyForecast,
} from '../services/weather';

export {
  axiosInstance,
  weatherService,
  getWeather,
  getCurrentWeather,
  getHourlyForecast,
};
